# rust-tree-sitter-md0.5

Compatibility package for the published `tree-sitter-md 0.5.3` grammar crate
required by ast-grep. The crate omits its MIT license file, so the spec adds the
matching file from the upstream release tag. Clean Fedora 43 and Fedora 44
x86_64 mock builds passed, and the package is enabled.
