name: rust-caseless0.2
status: enabled
blockers: []
validation:
  enablement_release: Release 0.2 uses the immutable static registry archive and enables configured SCM publication across Kreuzberg's six-cell target matrix; live build results are retained in the project playbook.
  source_identity: The static crates.io archive matches the pinned source SHA-256 and includes the declared MIT license file.
  dependency_evidence: caseless 0.2.2 requires unicode-normalization 0.1; Fedora 43, Fedora 44, and Rawhide provide a compatible crate(unicode-normalization) provider.
  duplicate_audit: No crate(caseless) provider was found in Fedora 43, Fedora 44, Rawhide, or matching RPM Fusion repositories as checked on 2026-07-18.
upstream:
  provider: crates_io
  crate: caseless
  version_requirement: = 0.2.2
  current_version: 0.2.2
  source_sha256: 8b6fd507454086c8edfd769ca6ada439193cdb209c7681712ef6275cccbfe5d8
  source_url_template: https://static.crates.io/crates/caseless/caseless-{version}.crate
copr:
  enabled: true
  chroots:
    - fedora-43-x86_64
    - fedora-43-aarch64
    - fedora-44-x86_64
    - fedora-44-aarch64
    - fedora-rawhide-x86_64
    - fedora-rawhide-aarch64
  subdir: packages/rust-caseless0.2
  spec: rust-caseless0.2.spec
