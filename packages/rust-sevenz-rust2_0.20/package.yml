name: rust-sevenz-rust2_0.20
status: enabled
blockers: []
validation:
  enablement_release: Release 0.3 uses the immutable static registry archive and enables configured SCM publication across Kreuzberg's six-cell target matrix.
  source_identity: The static crates.io archive matches the pinned source SHA-256 and includes the declared Apache-2.0 license file.
  dependency_evidence: Fedora supplies every native default dependency and the retained brotli, deflate, and zstd optional surfaces in all selected releases; Rust 1.85 is below every target toolchain.
  patch_scope: Fedora-only patches omit unsupported WASM, LZ4, and NT-time interfaces and reuse the project license as a byte-identical test fixture so the crate-devel payload has no duplicate license file.
  duplicate_audit: No crate(sevenz-rust2) = 0.20.2 provider was found in Fedora 43, Fedora 44, Rawhide, or matching RPM Fusion repositories as checked on 2026-07-18.
upstream:
  provider: crates_io
  crate: sevenz-rust2
  version_requirement: = 0.20.2
  current_version: 0.20.2
  source_sha256: 29225600349ef74beda5a9fffb36ac660a24613c0bde9315d0c49be1d51e9c24
  source_url_template: https://static.crates.io/crates/sevenz-rust2/sevenz-rust2-{version}.crate
copr:
  enabled: true
  chroots:
    - fedora-43-x86_64
    - fedora-43-aarch64
    - fedora-44-x86_64
    - fedora-44-aarch64
    - fedora-rawhide-x86_64
    - fedora-rawhide-aarch64
  subdir: packages/rust-sevenz-rust2_0.20
  spec: rust-sevenz-rust2_0.20.spec
