name: python-docling-core
status: enabled
blockers: []
source_policy:
  base_only: Optional chunking, transformer, tokenizer, example, and model branches are not packaged.
  metadata_patches:
    - Declare the setuptools build backend omitted from the published pyproject metadata.
    - Add requests because the installed CLI imports docling_core.utils.file unconditionally.
    - Accept Typer 0.26 from Fedora Rawhide while retaining the existing CLI implementation.
  test_scope: Run complete self-contained test files from the sdist; omit files requiring excluded test/data or optional dependencies.
license_audit:
  expression: MIT
  license_file: LICENSE
duplicate_check:
  fedora_43_44: absent
  rpm_fusion_43_44: absent
validation:
  source_sha256: verified
  patches_zero_fuzz: required
  spec_parse: passed
  package_manifest: passed
  selected_tests: six complete self-contained files
  installed_smokes: document creation, JSON roundtrip, DocLang serialization, and both CLI help commands
  required_copr_matrix: Fedora 43, Fedora 44, and Rawhide on x86_64 and aarch64
  current_release_copr: pending
upstream:
  provider: pypi
  project: docling-core
  current_version: 2.88.0
  source_url: https://files.pythonhosted.org/packages/ef/e7/38b149398ef5d433689e2b3bc356a91e06d7c77bb89731f82f24ab3d3508/docling_core-2.88.0.tar.gz
  source_sha256: ffc67cb863f6f93a875dfcc3dd3ac109fff68abcadcb4c951036fae777d82796
  repository: docling-project/docling-core
copr:
  enabled: true
  subdir: packages/python-docling-core
  spec: python-docling-core.spec
