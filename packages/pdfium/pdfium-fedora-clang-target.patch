From: Marcin FM <marcin@lgic.pl>
Subject: [PATCH] Use Fedora's Clang target triple on x86_64

Fedora installs compiler-rt under x86_64-redhat-linux-gnu and configures Clang
for that target. Chromium's build files hardcode x86_64-unknown-linux-gnu,
which makes the system-toolchain build look for a nonexistent builtins archive.

--- a/build/config/clang/BUILD.gn
+++ b/build/config/clang/BUILD.gn
@@ -183,6 +183,6 @@ template("clang_lib") {
       } else if (is_linux || is_chromeos) {
         if (current_cpu == "x64") {
-          _dir = "x86_64-unknown-linux-gnu"
+          _dir = "x86_64-redhat-linux-gnu"
         } else if (current_cpu == "x86") {
           _dir = "i386-unknown-linux-gnu"
         } else if (current_cpu == "arm") {
--- a/build/config/compiler/BUILD.gn
+++ b/build/config/compiler/BUILD.gn
@@ -1291,2 +1291,2 @@
-        cflags += [ "--target=x86_64-unknown-linux-gnu" ]
-        ldflags += [ "--target=x86_64-unknown-linux-gnu" ]
+        cflags += [ "--target=x86_64-redhat-linux-gnu" ]
+        ldflags += [ "--target=x86_64-redhat-linux-gnu" ]
