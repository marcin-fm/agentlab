# rust-async-tungstenite0.32

Compatibility package for the published `async-tungstenite 0.32.1` crate
required by `tower-lsp-server 0.23`. Only the default feature chain needed by
that dependency is exposed. Generated with rust2rpm 28. Clean Fedora 43 and
Fedora 44 x86_64 mock builds passed, and the package is enabled.
