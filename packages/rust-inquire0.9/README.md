# rust-inquire0.9

Compatibility package for the published `inquire 0.9.4` crate required by
ast-grep. The crate metadata declares MIT but omits the license file, so the
spec adds the matching file from the published upstream tag. Clean Fedora 43
and Fedora 44 x86_64 mock builds passed, and the package is enabled.
