target_release: 1.6.1
status: blocked
python_requirement:
  upstream: '>= 3.11, < 3.15'
  retained_rawhide_candidate: '>= 3.11, < 3.16 through serena-python-3.15.patch'
upstream_exact_runtime_changes:
  mcp: '1.27.0 -> 1.28.1'
  anthropic: '0.59.0 -> 0.117.0'
selected_surface:
  desired: headless local stdio MCP with explicit system language-server commands
  upstream_configuration:
  - stdio transport is selectable
  - dashboard and GUI can be disabled by flags
  - telemetry can be disabled with SERENA_USAGE_REPORTING=false
  - explicit local language-server commands are supported
  unresolved_installed_surfaces:
  - dashboard and GUI launchers
  - JetBrains backend
  - project server and cross-project query tools
  - non-stdio transports
  - token estimators and external API integration
  - managed language-server acquisition
decision:
  substantive_patches: removed
  binary_build: prohibited
  reenable_when: upstream-supported selected surface and complete Fedora provider graph are proven, or explicit maintainer approval is recorded
