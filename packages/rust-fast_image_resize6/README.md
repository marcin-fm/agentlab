# rust-fast_image_resize6

## Finalization status

The package is enabled for Fedora 43, Fedora 44, and Rawhide on `x86_64` and
`aarch64` as a selected Kreuzberg dependency.

The exact crates.io `6.0.0` release supplies the library and both declared
license texts. Upstream image fixtures have unresolved redistribution licensing
and are omitted. A fresh Fedora 44 `x86_64` Mock build passed 55 fixture-free
unit, integration, and documentation tests, and all ten artifacts passed digest
verification and `rpmlint` without package-content findings.
