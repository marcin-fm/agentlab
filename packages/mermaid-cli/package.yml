name: mermaid-cli
status: enabled
blockers: []
source_policy:
  runtime_tree: private Node application closure
  generated_html: copied from the equivalent release source index.html
  browser: Fedora chromium-headless
  browser_downloads: forbidden
  native_payloads: forbidden
  networked_icon_packs: runtime opt-in only
  corresponding_sources: verified for generated-heavy Mermaid, ELK, ZenUML, and Font Awesome content
  browser_generated_assets: The selected readable ESM bundles retain source-path markers and are injected only into Chromium. Remaining minified release output is browser-only, exact corresponding source is retained, and rebuilding the full Mermaid, ELK, ZenUML, KaTeX, and Font Awesome frontend toolchains in this application package is impractical.
  locally_executed_javascript: The mmdc Node entry point and its direct Node-side dependencies are not minified.
  font_awesome_fonts: official 7.2.0 release data matched byte-for-byte to the three active runtime WOFF2 files
  generated_source_delivery: repository-backed COPR configured-SCM make_srpm generation from immutable audited inputs
  srpm_generation_network: allowed only while materializing sources in the COPR source builder
  target_build_network: forbidden
  generated_output_hashes_enforced: true
closure_validation:
  release: 11.16.0
  package_paths: 327
  unique_source_archives: 326
  supplemental_notices: 5
  preferred_sources: 7
  licenses_verified: true
  bundled_provides_verified: true
  offline_verified: true
  upstream_contact_recorded: true
  upstream_contact: https://github.com/mermaid-js/mermaid-cli/issues/830
build_validation:
  release: 11.16.0-1
  fedora_43_x86_64: passed
  fedora_44_x86_64: passed
  installed_payload_smoke: passed
  output_formats:
    - SVG
    - PNG
    - PDF
  font_awesome_styles:
    - solid
    - regular
    - brands
  rpmlint_errors: 0
  host_rpm_install: false
duplicate_check:
  checked: "2026-07-15"
  fedora_43_44: absent
  rpm_fusion_43_44: absent
upstream:
  provider: github
  repository: mermaid-js/mermaid-cli
  tag_prefix: ""
  current_version: 11.16.0
  source_url_template: https://github.com/mermaid-js/mermaid-cli/archive/refs/tags/{version}.tar.gz
  source_sha256: c95d8b32a604e8393d9cfea9ee54060752b1cf4ff291be80a0b749e96a42c0c2
copr:
  enabled: true
  subdir: packages/mermaid-cli
  spec: mermaid-cli.spec
