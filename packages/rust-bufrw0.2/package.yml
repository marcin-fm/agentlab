name: rust-bufrw0.2
status: enabled
blockers: []
validation:
  enablement_release: Release 0.3 uses the immutable static registry archive and enables configured SCM publication across Kreuzberg's six-cell target matrix.
  source_identity: The static crates.io archive matches the pinned source SHA-256 and includes the declared MIT license file.
  dependency_evidence: The normalized crate manifest has no runtime dependencies; the retained metadata patch removes only benchmark-only Criterion metadata.
  duplicate_audit: No crate(bufrw) = 0.2.0 provider was found in Fedora 43, Fedora 44, Rawhide, or matching RPM Fusion repositories as checked on 2026-07-18.
upstream: {provider: crates_io, crate: bufrw, version_requirement: '= 0.2.0', current_version: 0.2.0, source_sha256: 394c22b416e3981206df472c3bcbbf9ae130883619aefc2199e6087517af1bba, source_url_template: 'https://static.crates.io/crates/bufrw/bufrw-{version}.crate'}
copr:
  enabled: true
  chroots:
    - fedora-43-x86_64
    - fedora-43-aarch64
    - fedora-44-x86_64
    - fedora-44-aarch64
    - fedora-rawhide-x86_64
    - fedora-rawhide-aarch64
  subdir: packages/rust-bufrw0.2
  spec: rust-bufrw0.2.spec
