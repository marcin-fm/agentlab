# rust-piston-float1

## Publication status

The package is independently eligible and enabled for configured SCM COPR
publication. The canonical crates.io archive is pinned by SHA-256, fetched
directly from the immutable static registry endpoint, and includes the declared
MIT license file.

No `crate(piston-float)` provider exists in Fedora 43, Fedora 44, Rawhide, or
matching RPM Fusion repositories. The normalized crate manifest has no external
dependencies. This package is published before `rust-vecmath1`, which requires
the compatible 1.x branch. Exact-current build and artifact-lint results are
retained in the project playbook.
