name: rust-rake0.3
status: enabled
blockers: []
validation:
  enablement_release: Release 0.2 uses the immutable static registry archive and enables configured SCM publication across Kreuzberg's six-cell target matrix.
  source_identity: The static crates.io archive matches the pinned source SHA-256 and includes the declared MIT and Apache-2.0 license files.
  dependency_evidence: Fedora provides lazy_static 1, regex 1, and serde 1 in every selected chroot.
  duplicate_audit: No crate(rake) = 0.3.6 provider was found in Fedora 43, Fedora 44, Rawhide, or matching RPM Fusion repositories as checked on 2026-07-18.
upstream:
  provider: crates_io
  crate: rake
  version_requirement: = 0.3.6
  current_version: 0.3.6
  source_sha256: 5a0a7b4878cdfa9c73657cf8479a1f2430104b21991db7940e97ab000056f0a1
  source_url_template: https://static.crates.io/crates/rake/rake-{version}.crate
copr:
  enabled: true
  chroots:
    - fedora-43-x86_64
    - fedora-43-aarch64
    - fedora-44-x86_64
    - fedora-44-aarch64
    - fedora-rawhide-x86_64
    - fedora-rawhide-aarch64
  subdir: packages/rust-rake0.3
  spec: rust-rake0.3.spec
