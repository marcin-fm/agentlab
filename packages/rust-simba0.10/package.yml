name: rust-simba0.10
status: enabled
blockers: []
validation:
  enablement_release: Release 0.3 uses the immutable static registry archive and enables configured SCM publication across Kreuzberg's six-cell target matrix.
  source_identity: The static crates.io archive matches the pinned source SHA-256 and includes the declared Apache-2.0 license file.
  dependency_evidence: Fedora provides approx 0.5, num-complex 0.4, and num-traits 0.2 in every selected release; Agentlab provides wide 1 across all six chroots.
  patch_scope: The Fedora-only metadata patch retains only the stable default/std surface required by nalgebra and omits unselected optional branches.
  duplicate_audit: No crate(simba) = 0.10.0 provider was found in Fedora 43, Fedora 44, Rawhide, or matching RPM Fusion repositories as checked on 2026-07-18.
upstream: {provider: crates_io, crate: simba, version_requirement: '= 0.10.0', current_version: 0.10.0, source_sha256: 8f45c644a9f3a386f9288625d9f0c1e999e1acf07a37df35d0516c7f199d9cb2, source_url_template: 'https://static.crates.io/crates/simba/simba-{version}.crate'}
copr:
  enabled: true
  chroots:
    - fedora-43-x86_64
    - fedora-43-aarch64
    - fedora-44-x86_64
    - fedora-44-aarch64
    - fedora-rawhide-x86_64
    - fedora-rawhide-aarch64
  subdir: packages/rust-simba0.10
  spec: rust-simba0.10.spec
