name: rust-jetscii0.5
status: enabled
blockers: []
validation:
  enablement_release: Release 0.2 uses the immutable static registry archive and enables configured SCM publication across Kreuzberg's six-cell target matrix.
  source_identity: The static crates.io archive matches the pinned source SHA-256 and includes the declared MIT and Apache-2.0 license files.
  dependency_evidence: The library has no runtime Cargo dependencies; Fedora supplies lazy_static 1, memmap2 0.7.1, proptest 1, and region 3 for the retained test suite in every selected release.
  patch_scope: The Fedora-only metadata patches replace the unmaintained dev-only memmap crate with Fedora's API-compatible memmap2 0.7.1 branch and remove two nightly-only, unselected feature interfaces without changing library sources.
  duplicate_audit: No crate(jetscii) = 0.5.3 provider was found in Fedora 43, Fedora 44, Rawhide, or matching RPM Fusion repositories as checked on 2026-07-18.
upstream: {provider: crates_io, crate: jetscii, version_requirement: '= 0.5.3', current_version: 0.5.3, source_sha256: 47f142fe24a9c9944451e8349de0a56af5f3e7226dc46f3ed4d4ecc0b85af75e, source_url_template: 'https://static.crates.io/crates/jetscii/jetscii-{version}.crate'}
copr:
  enabled: true
  chroots:
    - fedora-43-x86_64
    - fedora-43-aarch64
    - fedora-44-x86_64
    - fedora-44-aarch64
    - fedora-rawhide-x86_64
    - fedora-rawhide-aarch64
  subdir: packages/rust-jetscii0.5
  spec: rust-jetscii0.5.spec
