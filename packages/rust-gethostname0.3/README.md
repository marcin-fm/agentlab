# rust-gethostname0.3

Compatibility package for the exact published `gethostname 0.3.0` release
locked by `x11rb 0.12.0`. The generated metadata patch removes only the
Windows dependency. Clean Fedora 43 and Fedora 44 x86_64 mock builds passed,
and the package is enabled.
