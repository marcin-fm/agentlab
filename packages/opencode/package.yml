---
name: opencode
status: blocked
blockers:
- Fedora-source-built Bun 1.3.14 is unavailable.
- The normalized Bun compiler graph maps 3,895 contributing inputs to 531 package paths, 491 public npm name/version identities, and 12 OpenCode workspaces. Manual bundled Node Provides are generated from that exact map, but the final aggregate license expression and payload remain incomplete.
- The complete 829-archive source-license set is Fedora-classified. Binary selection narrows the package-local text work from 28 source-set gaps to 13 shipped npm identities; release 0.13 installs exact evidence for ten and retains three upstream notice holds.
- Release 0.17 refreshes the fail-closed final-license preflight after Bun resolves constants-browserify's exact package-local MIT notice. It preserves the three OpenCode notice holds, Bun's six WebKit fork-file holds and sole peechy npm text gap, and false final aggregate SPDX and RPM payload claims.
- The immutable Models.dev snapshot is source-built, supplied through OpenCode's supported local override, and bound as an exact supplemental compiler input.
- The native review classifies all 22 prebuilt payloads. OpenTUI, Parcel watcher, and bun-pty are source-built and present in the exact compiler input graph; F43/F44 clean-build results and final aggregate licensing remain unverified. The bun-pty vendor archive is generated during configured-SCM source preparation. FFF is omitted through the documented system-ripgrep fallback patch.
- The WASM review classifies all 14 payloads, and every selected functional WASM now has a verified source-build replacement. Photon's crates.io source is bound to npm's exact gitHead, its Cargo and wasm-bindgen closures are vendored, and two path-distinct Fedora builds are byte-identical after targeted source-path remapping with decode/resize/encode parity against the published module.
- The complete checked npm dependency tree and all 14 selected source-workspace links materialize without a package manager, dependency resolution, lifecycle hooks, or network access. Release 0.14 passes a local Fedora 44 x86_64 network-isolated build, check, exact 491-capability RPM metadata audit, resolved license payload plus Photon source-license audit, packaged-receipt comparison, and extracted-RPM version smoke; clean Mock/COPR results remain unverified.
- The 829-source production/build and test-capable bundles plus the bun-pty, Photon, and wasm-bindgen CLI Cargo vendor archives are generated by the configured-SCM source job and included in the source RPM; source preparation and local binary packaging pass without installing an RPM.
artifacts:
  selected_lock_audit: opencode-1.18.5-selected-lock-audit.json
  source_audit: opencode-1.18.5-source-audit.json
  license_review: license-review.yml
  native_review: native-review.yml
  models_snapshot_proof: models-snapshot-proof.json
  source_license_set_proof: source-license-set-proof.json
  source_materialization: opencode-1.18.5-source-materialization.json
  binary_embedding: opencode-1.18.5-binary-embedding.json
  final_license_closure: opencode-1.18.5-final-license-closure.json
upstream:
  provider: github
  repository: anomalyco/opencode
  tag_prefix: v
  current_version: 1.18.5
  source_url_template: https://github.com/anomalyco/opencode/archive/refs/tags/v{version}.tar.gz
  source_sha256: eb3daee12da937a36c3276efda2ce1253d3c8fbe2828ebd581a39a2c2d3efdab
  source_tag: v1.18.5
  source_commit: e5cc278dec9294a627a7b05f47ce6a564408c1a2
duplicate_check:
  checked: "2026-07-25"
  fedora_43_44_rawhide: absent
  rpm_fusion_43_44: absent
copr:
  enabled: false
  subdir: packages/opencode
  spec: opencode.spec
