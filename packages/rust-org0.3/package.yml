name: rust-org0.3
status: enabled
blockers: []
validation:
  enablement_release: Release 0.2 uses the immutable static registry archive and enables configured SCM publication across Kreuzberg's six-cell target matrix.
  source_identity: The static crates.io archive matches the pinned source SHA-256 and includes the declared MIT license file.
  dependency_evidence: The normalized crate manifest has no external dependencies.
  duplicate_audit: No crate(org) = 0.3.1 provider was found in Fedora 43, Fedora 44, Rawhide, or matching RPM Fusion repositories as checked on 2026-07-18.
upstream:
  provider: crates_io
  crate: org
  version_requirement: = 0.3.1
  current_version: 0.3.1
  source_sha256: cfdf547b633735ad9d67353aba48b3e685ab5ffb3195aaa9a1b1d8613e11b98c
  source_url_template: https://static.crates.io/crates/org/org-{version}.crate
copr:
  enabled: true
  chroots:
    - fedora-43-x86_64
    - fedora-43-aarch64
    - fedora-44-x86_64
    - fedora-44-aarch64
    - fedora-rawhide-x86_64
    - fedora-rawhide-aarch64
  subdir: packages/rust-org0.3
  spec: rust-org0.3.spec
