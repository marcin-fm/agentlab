name: rust-weezl0.2
status: enabled
blockers: []
validation:
  enablement_release: Release 0.2 uses the immutable static registry archive and enables configured SCM publication across Kreuzberg's six-cell target matrix.
  source_identity: The static crates.io archive matches the pinned source SHA-256 and includes the declared MIT and Apache-2.0 license files.
  dependency_evidence: The default library has no external dependency; Fedora provides futures 0.3, tokio 1, and tokio-util 0.6 for the optional async and test metadata, and every target toolchain exceeds the explicit Rust 1.88 MSRV.
  patch_scope: Separate Fedora-only patches remove benchmark-only dependencies and replace only the missing published LZW test fixture with data generated by the library encoder; no production source is changed.
  duplicate_audit: No crate(weezl) = 0.2.1 provider was found in Fedora 43, Fedora 44, Rawhide, or matching RPM Fusion repositories as checked on 2026-07-18.
upstream: {provider: crates_io, crate: weezl, version_requirement: '= 0.2.1', current_version: 0.2.1, source_sha256: d4ca08e5ef825b65b056d9efbd95c8750683f0a6d0466d02e96dc2e4e360f3d2, source_url_template: 'https://static.crates.io/crates/weezl/weezl-{version}.crate'}
copr:
  enabled: true
  chroots:
    - fedora-43-x86_64
    - fedora-43-aarch64
    - fedora-44-x86_64
    - fedora-44-aarch64
    - fedora-rawhide-x86_64
    - fedora-rawhide-aarch64
  subdir: packages/rust-weezl0.2
  spec: rust-weezl0.2.spec
